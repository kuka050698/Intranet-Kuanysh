<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kuka0.intranet">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".Activities.MainActivity" />
        <activity
            android:name=".Activities.LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.RegistrationActivity" />
        <activity android:name=".Activities.ProfileActivity" />
        <activity android:name=".Activities.AddItemActivity" />
        <activity
            android:name=".Activities.SplashActivity"
            android:theme="@style/SplashTheme" />
        <activity android:name=".Activities.StudentSignUpActivity" />
        <activity android:name=".Activities.TeahcerSignUpActivity" />
        <activity android:name=".Activities.AdminSignUpActivity" />
        <activity android:name=".Activities.InsertingCourseActivity" />
        <activity android:name=".Activities.StudentMainActivity" />
        <activity android:name=".Activities.TeacherMainActivity" />
        <activity android:name=".Activities.AdminMainActivity" />
        <activity android:name=".Activities.TeachersCourseListActivity" />
        <activity android:name=".Activities.CoursesListActivity" />
        <activity android:name=".Activities.CoursesDetailsActivity" />
        <activity android:name=".Activities.StudentCourseListDetailsActitivity" />
        <activity android:name=".Activities.StudentListActivity" />
        <activity android:name=".Activities.MyMarksActivity"></activity>
    </application>

</manifest>